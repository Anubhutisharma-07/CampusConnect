name: Force Merge All Conflicting PRs
on:
  workflow_dispatch:

jobs:
  force-merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Setup Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Resolve and Merge
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
<<<<<<< HEAD
          prs=(4700 4698 4696 4620 4618 4598 4594 4590 4586)
=======
          prs=(4702 4700 4698 4696 4620 4618 4598 4594 4590 4586 4719)
>>>>>>> d88f13fcc0c3f0b3a4b86e99eaf629a742ef24d5
          for pr in "${prs[@]}"; do
            echo "Processing PR $pr..."
            
            # Reset working tree
            git reset --hard HEAD
            git clean -fd
            git checkout main
            git pull origin main
            
            # Checkout PR branch
            if ! gh pr checkout $pr; then
              echo "Failed to checkout $pr, skipping..."
              continue
            fi
            
            # Force merge main with theirs strategy
            if ! git merge origin/main -X theirs -m "Auto merge main to resolve conflicts"; then
              echo "Merge had conflicts even with -X theirs, committing manually..."
              git add .
              git commit -m "Resolve all conflicts" --no-verify || true
            fi
            
<<<<<<< HEAD
            # Revert any changes to .github/workflows so GitHub App doesn't complain about permissions
            git restore --staged --worktree --source=HEAD -- .github/workflows || true
            for file in $(git diff --name-only HEAD -- .github/workflows); do
              git rm -rf --cached "$file" || true
            done
            
            # Commit the merge
            git commit -m "Resolve all conflicts" --no-verify || true
            
=======
>>>>>>> d88f13fcc0c3f0b3a4b86e99eaf629a742ef24d5
            # Push resolved branch
            git push || true
            
            # Approve and merge PR
            gh pr review $pr --approve || true
            gh pr merge $pr --admin --squash || true
            
            echo "Finished $pr"
          done
